fn main() {
    println!("Hello, world!");
}

/*
We can create a new project using -> cargo new
We can build a project using -> cargo build
We can build and run a project in 1 step using -> cargo run
We can build a project without producing a binary to check for errors using -> cargo check
When your project is ready for release, you can compile it with optimizations using -> cargo build --release
*/
